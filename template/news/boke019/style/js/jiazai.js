//????????????
$(function(){	
    var cubuk_seviye = $(document).scrollTop();
    var header_yuksekligi = $('.header-web').outerHeight();

    $(window).scroll(function() {
        var kaydirma_cubugu = $(document).scrollTop();

        if (kaydirma_cubugu > header_yuksekligi){$('.header-web').addClass('gizle');} 
        else {$('.header-web').removeClass('gizle');}

        if (kaydirma_cubugu > cubuk_seviye){$('.header-web').removeClass('sabit');} 
        else {$('.header-web').addClass('sabit');}				

        cubuk_seviye = $(document).scrollTop();	
     });
});

//????????????
 jQuery(document).ready(function($) {
        var loading=false
  $('div#post-read-more a').click( function() {
        if(loading)return
        $this = $(this);
        var href = $this.attr("href"); //??????????????????????????????
        if (href != undefined) { //??????????????????
            loading=true
            $this.addClass('loading').text("Loading"); //???a??????????????????loading???class?????????????????????????????????????????????
            $.ajax( { //??????ajax??????
                    url: href, //???????????????????????????????????????
                    type: "get", //???????????????get
                    error: function(request) {
                        loading=false
                    },
                    success: function(data) { //????????????
                        loading=false
                        $this.removeClass('loading').text("下一页"); //??????loading??????
                        var $res = $(data).find(".list"); //????????????????????????????????????????????????????????????
                        $('.box').append($res); //?????????????????????posts-loop???????????????
                        var newhref = $(data).find("#post-read-more a").attr("href"); //???????????????????????????
                        if( newhref != undefined ){
                            $("#post-read-more a").attr("href",newhref);
							
                        }else{
						  loading=false
							$("#post-read-more a").html("下一页").removeAttr("href").removeClass('loading');
							
							
                        }
                    }
        });   
    }   
    return false;   
});   
  
});  
 